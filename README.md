# calc_reactjs

# 📟 React Calculator – Vite + Vitest + Storybook

**Carpeta raíz del código:** `calc/`  
## 🚀 Puesta en marcha rápida

```bash
git clone <https://github.com/iancumes/calc_reactjs.git>
cd calc               # ↳ entrar a la carpeta del proyecto
npm install           # instala dependencias
npm run dev           # 🔥 servidor de desarrollo http://localhost:5173

| Comando                   | Descripción                                                                 |
| ------------------------- | --------------------------------------------------------------------------- |
| `npm run dev`             | Servidor Vite con *hot-reload*.                                             |
| `npm run build`           | Compilación optimizada en `dist/`.                                          |
| `npm run preview`         | Sirve la build localmente.                                                  |
| `npm test`                | Ejecuta Vitest (modo CI).                                                   |
| `npm run lint`            | ESLint Standard **sin “;”** (`semi: never`). El build falla si hay errores. |
| `npm run storybook`       | Storybook en `http://localhost:6006`.                                       |
| `npm run build-storybook` | Exporta Storybook estático en `storybook-static/`.                          |
